services:
  - type: web
    name: study-payment
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn min_project.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DEBUG
        value: false
